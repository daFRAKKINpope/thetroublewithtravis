# The Trouble With Travis

Edited and hosted with hugo. Hugo deposits into `/docs` via `hugo.toml` so github can host without building. Build locall with `hugo` then push.

Other useful commands 
`hugo new post post_name.md`

About section hosted in `/content/en/pages`
Posts section hosted in `/content/en/posts`
Images hosted in `/assets`
